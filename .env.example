# Local development only.
# Use this when running the Go app directly on your machine:
# go run ./cmd/server
DATABASE_URL=postgres://postgres:postgres@localhost:5432/usersdb?sslmode=disable

PORT=8080
APP_ENV=development